import { Navbar } from "../components/Navbar/Navbar";
import { Home } from "../pages/Home";

const Layout = () => {
  return (
    <main>
      <Navbar />
      <Home />
    </main>
  );
};

export default Layout;
